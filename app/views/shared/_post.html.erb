<li class="post--brief">
  <h3>
    <%= link_to post.title, user_post_path(post.user.id, post.id) %>
  </h3>
  <p class="post--text"> <%= post.text %> </p>
  <span class="comments--likes">
    Comments: <%= post.comments_counter || 0 %> ,
    Likes: <%= post.likes_counter || 0 %>
  </span>
</li>
